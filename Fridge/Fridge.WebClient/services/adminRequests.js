angular.module('app')

.factory('adminRequests', function (baseUrl, requester, userSession) {

    var url = "";
    var data = {};
    var headers = "";

    var adminRequests = {
        

    };

    return adminRequests;
});